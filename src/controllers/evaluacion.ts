export const verEvaluacion = async(req : Request , res : Response)=>{
    try {
        /**... */

    } catch (error) {
        throw error;
    }
}
export const crearEvaluacion = async(req : Request , res : Response)=>{
    try {
        /**... */

    } catch (error) {
        throw error;
    }
}
export const editarEvaluacion = async(req : Request , res : Response)=>{
    try {
        /**... */

    } catch (error) {
        throw error;
    }
}
export const bajaEvaluacion = async(req : Request , res : Response)=>{
    try {
        /**... */

    } catch (error) {
        throw error;
    }
}

